import React from 'react'

const JSXRules = () => {
  return (
    <>
      <h2>JSX Rules</h2>
      <section>1. JSX must return a single parent element.</section>
      <section>2. JSX tags must be closed.</section>
      <section>3. JSX is case-sensitive.</section>
      <section>4. JSX can use JavaScript expressions.</section>
      <section>5. JSX attributes use camelCase naming convention.(class becomes className, for (in label) becomes htmlFor)</section>
    </>
  )
}

export default JSXRules